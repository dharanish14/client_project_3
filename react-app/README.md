# NUNP React + Vite App (scaffold)

Minimal scaffold for the NUNP site using React + Vite.

Getting started:

1. cd react-app
2. npm install
3. npm run dev

Port the static site's HTML into React components under `src/components/`.
